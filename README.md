palindrome-script 🚀

Script responsible to check if value is a palindrome.

## To run the script...

Make sure you have Node.js installed on your system. If you haven't already, download and install it from the official website: https://nodejs.org/

Then open a terminal or a command prompt and navigate to the directory and run the command:

```bash
$ node script.js
```